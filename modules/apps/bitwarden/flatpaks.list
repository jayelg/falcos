# Flatpaks to install on first boot.
# One flatpak ID per line. Blank lines and # comments are ignored.
# Processed at build time by run-module.sh: each module's
# flatpaks.list is concatenated into /usr/share/flatpak-defaults/apps.list
# and installed by the flatpak module's first-boot service.
com.bitwarden.desktop
