# dev-tools:latest

Development tools. `just` is a hard dependency of the goojust justfile framework, so it is installed by the `core/goojust` module rather than here.

**Packages:** direnv, git, git-credential-libsecret, git-delta, git-lfs
