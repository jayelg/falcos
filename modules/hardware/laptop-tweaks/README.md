# laptop-tweaks

Laptop-flavor kernel tweaks. Laptop flavor only (gated by the `[laptop]`
section in [image.kdl](../../../image.kdl)). Pure-file module.

## Files

- `kargs.d/10-sleep.toml` -- `mem_sleep_default=s2idle`
