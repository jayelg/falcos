[Unit]
Wants=network-online.target
After=network-online.target
StartLimitIntervalSec=600
StartLimitBurst=5

[Service]
Restart=on-failure
RestartSec=60s
# Stock unit runs `bootc upgrade --apply`, which reboots immediately once an
# update is fetched. Drop --apply so it only stages the update; the
# falcos-bootc-updates notifier (user session) then prompts to restart.
ExecStart=
ExecStart=/usr/bin/bootc upgrade --quiet
