# Anything under files/ is copied verbatim into the image root, preserving
# this path — this file lands at /usr/share/falcos-template/example.conf.
# Use real target paths (etc/, usr/lib/, usr/share/, ...). No build logic
# runs on these; they are a plain overlay.
example_setting = true
