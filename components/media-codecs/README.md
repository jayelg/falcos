# multimedia:latest

Multimedia codecs and PipeWire extras via the negativo17 fedora-multimedia repo.

**Repo:** negativo17 fedora-multimedia (priority=90)

**Overrides (distro-sync + versionlock):** mesa, libva, intel-media-driver, libheif

**Packages:** ffmpeg, ffmpeg-libs, libva-utils, pipewire-config-raop, pipewire-gstreamer, pipewire-libs-extra, pipewire-utils, mesa-libOpenCL (x86_64 pinned)
